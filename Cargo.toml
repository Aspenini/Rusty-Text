[package]
name = "rusty-text"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Repeat a phrase to a file; streams so huge counts stay memory-safe"
license = "MIT"
readme = "README.md"

[lints.rust]
unsafe_code = "forbid"
